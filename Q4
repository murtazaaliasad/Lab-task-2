#include <stdio.h>

int main()
{
    int age, months;

    printf("Enter your age in years: ");
    scanf("%d", &age);

    months = age * 12;

    printf("Your age in months is: %d\n", months);

    return 0;
}
